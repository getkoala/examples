import type { Profile } from './indexing';
export declare function seen(profile: Profile, path: string, times?: number | undefined): boolean;
export declare const viewed: typeof seen;
export declare function viewing(_profile: Profile, path: string): boolean;
export declare class Page {
    private profile;
    constructor(profile: Profile);
    seen(path: string, times?: number | undefined): boolean;
    viewed(path: string): boolean;
    viewing(path: string): boolean;
    get not(): {
        seen: (path: string, times?: number | undefined) => boolean;
        viewed: (path: string) => boolean;
        viewing: (path: string) => boolean;
    };
}
