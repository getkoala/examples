export interface JsBloomOptions {
    size: number;
    hashes: number;
    seed?: number;
    bits?: null | number[];
}
export declare class JsBloom {
    private options;
    private bits;
    constructor(options?: Partial<JsBloomOptions>);
    add(...keys: string[]): void;
    test(...keys: string[]): boolean;
    toHash(): JsBloomOptions;
    toJson(): string;
    indexesFor(key: string): number[];
}
