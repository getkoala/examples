export interface CountMinSketchOptions {
    size: number;
    hashes: number;
    data?: number[][];
    seeds?: number[];
}
export declare class CountMinSketch {
    private options;
    private k;
    private m;
    private data;
    private seeds;
    constructor(options?: Partial<CountMinSketchOptions>);
    count(item: string): number;
    add(item: string, n?: number): number;
    setCount(item: string, n: number): void;
    toHash(): object;
    toJSON(): string;
}
